spring:
  application:
    name: admin-service
  cloud:
    nacos:
      server-addr: nacos-server:8848
      config:
        file-extension: yaml
  profiles:
    active: dev

# pull config file, using data_id: admin-service-dev.yaml